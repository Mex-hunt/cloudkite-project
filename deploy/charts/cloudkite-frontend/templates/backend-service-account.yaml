{{- if .Values.backend.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloudkite-backend
  labels:
    {{- include "cloudkite-frontend.labels" . | nindent 4 }}
  annotations:
    iam.gke.io/gcp-service-account: {{ .Values.backend.googleServiceAccount | quote }}
{{- end }}
